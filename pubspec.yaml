name: wrappers
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_touch_ripple: ^2.2.32

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
